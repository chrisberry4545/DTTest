'use strict';

describe('dtTestApp.mainTable', function() {

  beforeEach(module('dtTestApp.mainTable'));

  describe('main table controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mainTableViewController = $controller('MainTableViewController');
      expect(mainTableViewController).toBeDefined();
    }));

  });
});